name: modbus_client_tcp
description: This is the Modbus Client package sending requests to a remote device via TCP/IP connection.
version: 1.2.1
repository: https://github.com/cabbi/modbus_client_tcp

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  logging: ^1.2.0
  synchronized: ^3.1.0
  modbus_client: ^1.2.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.21.0
